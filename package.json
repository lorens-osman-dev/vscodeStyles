{
  "name": "new-style",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "build": "bun build src/ts/style.ts --outdir=dist ",
    "ts": "bun build src/ts/style.ts --outdir=dist --watch",
    "sass": "sass -w ./src/sass/style.scss ./dist/style.css "
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "sass": "^1.87.0"
  }
}
